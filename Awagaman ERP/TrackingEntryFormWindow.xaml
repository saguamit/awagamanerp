<Controls:MetroWindow x:Class="Awagaman_ERP.TrackingEntryFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Tracking Entry Details" Height="650" Width="800" WindowStartupLocation="CenterOwner"
        Background="#EEEEEE" Focusable="True" Loaded="Window_Loaded">
    <Controls:MetroWindow.Resources>
        <SolidColorBrush x:Key="ThemeBlue" Color="#18609A" />
        <SolidColorBrush x:Key="ThemeGreen" Color="#00B08B" />
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="26" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="BorderBrush" Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="26" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="BorderBrush" Value="#CCCCCC" />
        </Style>
    </Controls:MetroWindow.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tracking Entry" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ThemeBlue}" Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="White" Padding="16" Margin="0,0,0,12" BorderBrush="#DDDDDD" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Challan Details" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource ThemeBlue}" Margin="0,0,0,12" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Challan No." Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding ChallanNo}" Style="{StaticResource InputStyle}" IsReadOnly="True" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Challan Date" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding ChallanDate}" Style="{StaticResource DatePickerStyle}" IsEnabled="False" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="From" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding From}" Style="{StaticResource InputStyle}" IsReadOnly="True" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="To" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding To}" Style="{StaticResource InputStyle}" IsReadOnly="True" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Vehicle No." Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding VehicleNo}" Style="{StaticResource InputStyle}" IsReadOnly="True" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Driver Mobile" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding DriverMobile}" Style="{StaticResource InputStyle}" IsReadOnly="True" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="White" Padding="16" Margin="0,0,0,12" BorderBrush="#DDDDDD" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Dispatch &amp; Delivery" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource ThemeBlue}" Margin="0,0,0,12" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="E-way Bill Till Date" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding EwayBillTillDate}" Style="{StaticResource DatePickerStyle}" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Dispatch Date" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding DispatchDate}" Style="{StaticResource DatePickerStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Dispatch Time" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding DispatchTime, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" />
                            </StackPanel>
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Delivered Date" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding DeliveredDate}" Style="{StaticResource DatePickerStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Delivered Time" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding DeliveredTime, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="#FFF3E0" Padding="16" BorderBrush="#FFE0B2" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Reporting Track" FontSize="14" FontWeight="SemiBold" Foreground="#E65100" Margin="0,0,0,12" />
                        <ListBox x:Name="ReportingTracksListBox" DisplayMemberPath="DisplayText" Height="150" BorderBrush="#CCCCCC" Margin="0,0,0,8" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ReportRemarksBox" Grid.Column="0" Style="{StaticResource InputStyle}" Text="Enter update..." GotFocus="ReportRemarksBox_GotFocus" />
                            <Button Grid.Column="2" Content="Add Update" Background="{StaticResource ThemeGreen}" Foreground="White" BorderThickness="0" Padding="12,6" FontSize="12" Cursor="Hand" Click="AddReport_Click" />
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Margin="0,16,0,0" Padding="0,12,0,0" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Background="#E0E0E0" Foreground="#333333" BorderThickness="0" Padding="20,8" FontSize="13" Cursor="Hand" Margin="0,0,12,0" Click="Cancel_Click" />
                <Button Content="Save" Background="{StaticResource ThemeGreen}" Foreground="White" BorderThickness="0" Padding="20,8" FontSize="13" Cursor="Hand" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Controls:MetroWindow>
