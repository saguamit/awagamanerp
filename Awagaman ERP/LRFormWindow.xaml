<Controls:MetroWindow x:Class="Awagaman_ERP.LRFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Awagaman_ERP"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="LR Entry"
        Height="760"
        Width="1180"
        MinWidth="1040"
        MinHeight="700"
        WindowState="Normal"
        WindowStartupLocation="CenterOwner"
        Background="#EEEEEE"
        BorderThickness="1"
        BorderBrush="#18609A"
        PreviewKeyDown="LRFormWindow_PreviewKeyDown">
    <Controls:MetroWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <SolidColorBrush x:Key="ThemeBlue" Color="#18609A" />
        <SolidColorBrush x:Key="ThemeGreen" Color="#00B08B" />
        <SolidColorBrush x:Key="CardBorderBrush" Color="#DDDDDD" />
        <SolidColorBrush x:Key="StrongTextBrush" Color="#333333" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#666666" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource ThemeBlue}" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource StrongTextBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="26" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="BorderBrush" Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="InputDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="26" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="BorderBrush" Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ThemeGreen}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0" />
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Controls:MetroWindow.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Create New LR" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource ThemeBlue}" />
            <Border Grid.Column="1" Background="#E8F4FD" BorderBrush="#B6D8F2" BorderThickness="1" Padding="12,6" CornerRadius="4"
                    Visibility="{Binding HasChallanLorryHire, Converter={StaticResource BoolToVis}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Challan Lorry Hire: " Foreground="#18609A" FontSize="12" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding ChallanLorryHire, StringFormat=₹ {0:N2}}" Foreground="#18609A" FontSize="13" FontWeight="Bold" />
                </StackPanel>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,0,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <!-- Section 1: Basic Info -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16" Background="#F5FAFF" BorderBrush="#D5E6F6">
                        <StackPanel>
                        <TextBlock Text="Basic Details" Style="{StaticResource SectionHeaderStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="LR No." Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.LRNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" LostFocus="LRNo_LostFocus" />
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Date" Style="{StaticResource FieldLabelStyle}" />
                                <DatePicker SelectedDate="{Binding CurrentEntry.Date, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputDatePickerStyle}" />
                            </StackPanel>
                            
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Vehicle No." Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.VehicleNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" ToolTip="Editable from Challan only" />
                            </StackPanel>
                        </Grid>
                        
                        <Grid Margin="0,14,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Vehicle Type" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.VehicleType, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" ToolTip="Editable from Challan only" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="From" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.From, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" ToolTip="Editable from Challan only" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="To" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.To, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" ToolTip="Editable from Challan only" />
                            </StackPanel>
                        </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Section 2: Consignor & Consignee -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Background="#F7FFF8" BorderBrush="#CFEBD3">
                        <StackPanel>
                            <TextBlock Text="Consignor" Style="{StaticResource SectionHeaderStyle}" />
                            <TextBlock Text="Name" Style="{StaticResource FieldLabelStyle}" />
                            <Grid>
                                <TextBox x:Name="ConsignorNameBox" Text="{Binding CurrentEntry.ConsignorName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,12" LostFocus="PartyName_LostFocus" TextChanged="ConsignorName_TextChanged" />
                                <Popup x:Name="ConsignorPopup" PlacementTarget="{Binding ElementName=ConsignorNameBox}" Placement="Bottom" StaysOpen="False" Width="{Binding ActualWidth, ElementName=ConsignorNameBox}">
                                    <Border BorderBrush="#AAA" BorderThickness="1" Background="White" CornerRadius="4">
                                        <ListBox x:Name="ConsignorSuggestionList" MaxHeight="200" FontSize="13" SelectionChanged="ConsignorSuggestionList_SelectionChanged" />
                                    </Border>
                                </Popup>
                            </Grid>
                            <TextBlock Text="Address" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox x:Name="ConsignorAddressBox" Text="{Binding CurrentEntry.ConsignorAddress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,12" />
                            <TextBlock Text="GST" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox x:Name="ConsignorGSTBox" Text="{Binding CurrentEntry.ConsignorGST, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" CharacterCasing="Upper" />
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="2" Style="{StaticResource CardStyle}" Background="#FFFDF6" BorderBrush="#F0E2B8">
                        <StackPanel>
                            <TextBlock Text="Consignee" Style="{StaticResource SectionHeaderStyle}" />
                            <TextBlock Text="Name" Style="{StaticResource FieldLabelStyle}" />
                            <Grid>
                                <TextBox x:Name="ConsigneeNameBox" Text="{Binding CurrentEntry.ConsigneeName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,12" LostFocus="ConsigneeName_LostFocus" TextChanged="ConsigneeName_TextChanged" />
                                <Popup x:Name="ConsigneePopup" PlacementTarget="{Binding ElementName=ConsigneeNameBox}" Placement="Bottom" StaysOpen="False" Width="{Binding ActualWidth, ElementName=ConsigneeNameBox}">
                                    <Border BorderBrush="#AAA" BorderThickness="1" Background="White" CornerRadius="4">
                                        <ListBox x:Name="ConsigneeSuggestionList" MaxHeight="200" FontSize="13" SelectionChanged="ConsigneeSuggestionList_SelectionChanged" />
                                    </Border>
                                </Popup>
                            </Grid>
                            <TextBlock Text="Address" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding CurrentEntry.ConsigneeAddress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,12" />
                            <TextBlock Text="GST" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding CurrentEntry.ConsigneeGST, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" CharacterCasing="Upper" />
                        </StackPanel>
                    </Border>
                    </Grid>

                    <!-- Left-bottom: Challan lorry hire summary -->
                    <Border Style="{StaticResource CardStyle}" Background="#EDF5FF" BorderBrush="#CFE0F4">
                        <StackPanel>
                            <TextBlock Text="Challan Info" Style="{StaticResource SectionHeaderStyle}" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Lorry Hire (from CH No.)" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1"
                                           Text="{Binding ChallanLorryHire, StringFormat=₹ {0:N2}, TargetNullValue=₹ 0.00}"
                                           Foreground="#18609A"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <!-- Section 3: Shipment Details -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16" Background="#FFF8F4" BorderBrush="#F0D8C8">
                        <StackPanel>
                        <TextBlock Text="Shipment Details" Style="{StaticResource SectionHeaderStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="2*" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="L" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.SizeL, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="W" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.SizeW, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="H" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.SizeH, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                        </Grid>
                        
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Actual Weight" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.ActualWeight, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Charged Weight" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.ChargedWeight, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="No. of Pkg" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.PKG, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="To Pay / To Be Billed" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.PayType, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="CH No." Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.CHNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" IsReadOnly="True" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Invoice" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.Invoice, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Pkg Type" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.PkgType, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Value" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="DESCRIPTION" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.Description, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                            </StackPanel>
                        </Grid>
                        </StackPanel>
                    </Border>
                
                    <!-- Section 4: Freight Settlement -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16" Background="#F4FBF8" BorderBrush="#CEE8DC">
                        <StackPanel>
                        <TextBlock Text="Freight Settlement" Style="{StaticResource SectionHeaderStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Total Freight" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.TotalFreight, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Hamali" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.Hamali, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Detention" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.Detention, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                            <StackPanel Grid.Column="6">
                                <TextBlock Text="Others" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.Others, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" GotKeyboardFocus="NumericBox_GotKeyboardFocus" PreviewMouseLeftButtonDown="NumericBox_PreviewMouseLeftButtonDown" />
                            </StackPanel>
                        </Grid>

                        <Border Margin="0,14,0,0" Background="#F7FBFF" BorderBrush="#D9E8F5" BorderThickness="1" Padding="12,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Total Bill" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBlock Text="{Binding CurrentEntry.TotalBill, StringFormat=N2}" Foreground="{StaticResource ThemeBlue}" FontWeight="SemiBold" FontSize="16" Margin="0,2,0,0" />
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Bal" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBlock Text="{Binding CurrentEntry.Bal, StringFormat=N2}" Foreground="#00B08B" FontWeight="SemiBold" FontSize="16" Margin="0,2,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        </StackPanel>
                    </Border>

                    <!-- Section 5: Billing -->
                    <Border Style="{StaticResource CardStyle}" Background="#F9F6FF" BorderBrush="#DDD0F0">
                        <StackPanel>
                        <TextBlock Text="Billing" Style="{StaticResource SectionHeaderStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Bill Party" Style="{StaticResource FieldLabelStyle}" />
                                <TextBox Text="{Binding CurrentEntry.BillParty, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                            </StackPanel>
                        </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Margin="0,16,0,0" Padding="0,16,0,0" BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,12,0" Click="Cancel_Click" />
                <Button Content="Save LR" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Controls:MetroWindow>
